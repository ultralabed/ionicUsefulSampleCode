//To be able to use oauth function, you need to install inappbrowser and whitelist. Run the command as follow.
cordova plugin add org.apache.cordova.inappbrowser
cordova plugin add cordova-plugin-whitelist
